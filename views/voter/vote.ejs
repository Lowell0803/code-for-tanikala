<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Voting System</title>

    <link rel="stylesheet" type="text/css" href="voter/css/index_voter.css">
    <link rel="stylesheet" type="text/css" href="voter/css/vote.css">
    <link rel="stylesheet" type="text/css" href="voter/css/vote_choice.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&family=Poppins:wght@400;700&display=swap"
        rel="stylesheet">
</head>

<body>

    <header class="header">
        <div class="header-logo">
            <img src="img/logo_tanikala.png" alt="Tanikala Voting System" />
        </div>
        <nav class="header-nav">
            <ul class="header-nav-content">
                <li><a href="vote.html" id="active-link">Vote</a></li>
                <li><a href="review.html">Review</a></li>
                <li><a href="verify.html">Verify</a></li>
            </ul>
        </nav>
        <div class="header-bottom-line"></div>
    </header>

    <div class="content">
        <div class="content-header">
            <div class="content-header-bulsu">
                <img src="img/logo_bulsu.png">
            </div>
            <div class="content-header-text">
                <h1>BulSU Student Government Elections<br>S.Y. 2025 - 2026</h1>
            </div>
            <div class="content-header-ucse">
                <img src="img/logo_bulsu_ucse.png">
            </div>
        </div>
        <form>
            <div class="content-main">

                <p>You can submit your votes by pressing the SUBMIT BALLOT button below.</p>

                <div class="ballot ballot-ssc">
                    <br>
                    <h3 class="ballot-header ballot-council">Supreme Student Council</h3><br>

                    <!-- President Candidates -->
                    <h3 class="ballot-header ballot-position">President</h3><br>
                    <div class="container container-ssc" id="container-president">
                        <h2>President</h2>
                        <div>
                            <% candidates.filter(candidate=> candidate._id.startsWith('president_'))
                                .forEach((president, index) => { %>
                                <div class="candidate">
                                    <% if (president._id) { %>
                                        <h3>
                                            <%= president.name %>
                                        </h3>
                                        <p>Party: <%= president.party %>
                                        </p>
                                        <img src="<%= president.image %>" alt="<%= president.name %>">
                                        <button class="button" id="button-edit"
                                            onclick="toggleForm('<%= president._id %>', '<%= president.position %>', '<%= president.party %>', '<%= president.name %>', '<%= president.moreInfo %>')">Edit</button>
                                        <% } else { %>
                                            <p>No candidate</p>
                                            <button class="button" id="button-add">Add</button>
                                            <% } %>
                                </div>
                                <% }); %>
                        </div>
                    </div>



                    <br>

                    <!-- Vice President Candidates -->
                    <h3 class="ballot-header ballot-position">Vice President</h3><br>
                    <div id="ballot-vice-president" class="ballot-choices">
                        <!-- Loop through all vice president candidates -->
                        <% candidates .filter(candidate=> candidate.position === 'vice president')
                            .forEach((vicePresident, index) => { %>
                            <input id="vice-president-<%= index + 1 %>" type="radio" name="vice-president"
                                value="<%= vicePresident._id %>">
                            <label for="vice-president-<%= index + 1 %>">
                                <h4>
                                    <%= vicePresident.party %>
                                </h4>
                                <img src="<%= vicePresident.image %>" alt="<%= vicePresident.name %>">
                                <h4>
                                    <%= vicePresident.name %>
                                </h4>
                                <p>
                                    <%= vicePresident.moreInfo %>
                                </p>
                            </label>
                            <% }); %>
                    </div>


                    <br>

                    <h3 class="ballot-header ballot-position">Senators</h3>
                    <div id="ballot-senator" class="ballot-choices">
                        <% const sortedSenators=candidates .filter(candidate=>
                            candidate.position === 'senator')
                            .sort((a, b) => a.party.localeCompare(b.party));

                            sortedSenators.forEach((senator, index) => {
                            %>
                            <input id="senator-<%= index + 1 %>" type="radio" name="senator" value="<%= senator._id %>">
                            <label for="senator-<%= index + 1 %>">
                                <h4>
                                    <%= senator.party %>
                                </h4>
                                <img src="<%= senator.image %>" alt="<%= senator.name %>">
                                <h4>
                                    <%= senator.name %>
                                </h4>
                                <p>
                                    <%= senator.moreInfo %>
                                </p>
                            </label>
                            <% }); %>
                    </div>

                    <br>
                    <h3 class="ballot-header ballot-council">Local Student Council - <%= voterCollege %>
                    </h3><br>
                    <br>
                    <h3 class="ballot-header ballot-position">Governor</h3><br>
                    <div id="ballot-governor" class="ballot-choices">
                        <% let governorCandidates=candidates_lsc.filter(candidate=>
                            candidate.college === voterCollege &&
                            candidate.position === 'governor'
                            ); %>

                            <% governorCandidates.forEach((candidate, index)=> { %>
                                <input id="governor-<%= index + 1 %>" type="radio" name="governor"
                                    value="governor-<%= candidate.party.toLowerCase().replace(/\s/g, '') %>">
                                <label for="governor-<%= index + 1 %>">
                                    <h4>
                                        <%= candidate.party %>
                                    </h4>
                                    <img src="<%= candidate.image %>" alt="<%= candidate.name %>">
                                    <h4>
                                        <%= candidate.name %>
                                    </h4>
                                    <p>
                                        <%= candidate.moreInfo %>
                                    </p>
                                </label>
                                <% }) %>
                    </div>

                    <br>
                    <h3 class="ballot-header ballot-position">Vice Governor</h3><br>
                    <div id="ballot-vice-governor" class="ballot-choices">
                        <% let viceGovernorCandidates=candidates_lsc.filter(candidate=>
                            candidate.college === voterCollege &&
                            candidate.position === 'vice governor'
                            ); %>

                            <% viceGovernorCandidates.forEach((candidate, index)=> { %>
                                <input id="vice-governor-<%= index + 1 %>" type="radio" name="vice-governor"
                                    value="<%= candidate.party.toLowerCase().replace(/\s/g, '') %>">
                                <label for="vice-governor-<%= index + 1 %>">
                                    <h4>
                                        <%= candidate.party %>
                                    </h4>
                                    <img src="<%= candidate.image %>" alt="<%= candidate.name %>">
                                    <h4>
                                        <%= candidate.name %>
                                    </h4>
                                    <p>
                                        <%= candidate.moreInfo %>
                                    </p>
                                </label>
                                <% }) %>
                    </div>

                    <br>
                    <h3 class="ballot-header ballot-position">Board Member</h3><br>
                    <div id="ballot-board-member" class="ballot-choices">
                        <% let boardMemberCandidates=lsc_board_members.filter(candidate=>
                            candidate.college === voterCollege &&
                            candidate.program === voterProgram
                            ); %>

                            <% boardMemberCandidates.forEach((candidate, index)=> { %>
                                <input id="board-member-<%= index + 1 %>" type="radio" name="board-member"
                                    value="<%= candidate.party.toLowerCase().replace(/\s/g, '') %>">
                                <label for="board-member-<%= index + 1 %>">
                                    <h4>
                                        <%= candidate.party %>
                                    </h4>
                                    <img src="<%= candidate.image %>" alt="<%= candidate.name %>">
                                    <h4>
                                        <%= candidate.name %>
                                    </h4>
                                    <p>
                                        <%= candidate.moreInfo %>
                                    </p>
                                </label>
                                <% }) %>
                    </div>

                    <br>
                </div>
            </div>




            <p>You still need to review and confirm your votes in the next page.</p>
            <input type="submit" value="Submit Ballot">
    </div>
    </form>
    </div>


    <div class="footer">
        <div class="footer-left">
            <p>OCTOBER 23, 2024 - 04:17 AM</p>
        </div>
        <div class="footer-right">
            <p>© 2024 - Fourmula 1</p>
        </div>
    </div>

    <script>
    </script>

</body>

</html>